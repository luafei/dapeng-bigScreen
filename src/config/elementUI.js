import Vue from 'vue';
import {
    Dialog,
    Input,
    Button,
    ButtonGroup,
    Table,
    TableColumn,
    DatePicker,
    Row,
    Col,
    Select,
    Dropdown,
    DropdownMenu,
    DropdownItem,
    Option,
    Pagination
} from 'element-ui';
Vue.use(Dialog)
.use(Input)
.use(Button)
.use(ButtonGroup)
.use(Table)
.use(TableColumn)
.use(DatePicker)
.use(Row)
.use(Col)
.use(Select)
.use(Dropdown)
.use(DropdownMenu)
.use(DropdownItem)
.use(Option)
.use(Pagination)